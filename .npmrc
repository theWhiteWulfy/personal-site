# Cloudflare Pages runs `npm clean-install`, which enforces peer-dependency
# ranges strictly. The dependency tree contains peer ranges that are stale but
# functionally compatible (verified by build + test suite). Keep installs
# consistent between local dev and CI until a dependency refresh pass.
legacy-peer-deps=true
