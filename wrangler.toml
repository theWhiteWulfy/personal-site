name = "meteoricteachings"
compatibility_flags = [ "nodejs_compat" ]

# Required secrets (set via: wrangler secret put <KEY>)
# RESOURCE_SIGNING_SECRET - HMAC key for download token signing
# ADMIN_API_KEY           - Bearer token for admin API endpoints

# Optional secrets
# WEBMENTION_IO_TOKEN     - webmention.io API token (optional; authenticated API access)

[[d1_databases]]
binding = "DB"
database_name = "meteoric"
database_id = "8380ec22-098e-4814-a56f-48d907425b35"