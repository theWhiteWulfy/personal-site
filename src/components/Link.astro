---

type Props = {
  to: string;
  external?: boolean;
  underline?: boolean;
  group?: boolean;
  className?: string;
};

const {
  to,
  external,
  underline = true,
  group = false,
  className,
  ...rest
} = Astro.props;
---

<a
  href={to}
  target={external ? "_blank" : "_self"}
  class={className}
  {...rest}
>
  <slot />
</a>
