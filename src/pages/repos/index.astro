---
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";
import style from "@styles/archive.module.css";
import Entry from "@components/Entry.astro";
import { readingTime } from "@lib/utils";
import { entryPath } from "@lib/content-shims";
import NewsletterForm from "@components/custom/NewsletterForm.astro";

const data = (await getCollection("repos"))
  .filter((article) => !article.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<Layout
  title={`Repositories & Open Source`}
  path="/repos/"
  description="Open-source tools, repositories, and architectural experiments."
>
  <main class={style.main}>
    <div class={style.title}>
      <h1 class={style.heading}>
        <span>Repositories</span>
      </h1>
    </div>

    <div class={style.content}>
      <p>
        Open-source tooling, agent architectures, Model Context Protocol servers, and developer utilities.
      </p>

      <ul>
        {
          data.map((node) => (
            <Entry
              title={node.data.title}
              date={node.data.date.toString()}
              path={entryPath(node)}
              timeToRead={readingTime(node.body)}
              excerpt={node.data.excerpt}
              tags={node.data.tags}
            />
          ))
        }
      </ul>
      <NewsletterForm />
    </div>
  </main>
</Layout>
